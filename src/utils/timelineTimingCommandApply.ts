// clean catch-up 暂时沿用 Web 路径；时间命令 apply 只在 document-model 中实现一次。
export { applyTimelineTimingCommandToProject } from "@xiqu/document-model";
export type { TimelineTimingCommandApplyResult } from "@xiqu/document-model";
