// 普通标注事务 apply 已由共享包顺序执行，旧路径只保留 all-or-nothing 结果出口。
export { applyAnnotationTransactionCommandToProject } from "@xiqu/document-model";
export type { AnnotationTransactionCommandApplyResult } from "@xiqu/document-model";
